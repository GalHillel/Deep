"""
deep.objects
~~~~~~~~~~~~

Pure-Python object layer for Deep.

This package provides pure-Python implementations of the standard VCS object format,
packfile parser/writer, delta engine, and pack index — enabling Deep to
interoperate with standard remote servers (GitHub, GitLab) over SSH and HTTPS
without any external CLI or library dependency.
"""
