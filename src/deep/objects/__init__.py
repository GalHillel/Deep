"""
deep.objects
~~~~~~~~~~~~

Git-compatible object layer for Deep.

This package provides pure-Python implementations of the Git object format,
packfile parser/writer, delta engine, and pack index — enabling Deep to
interoperate with standard Git servers (GitHub, GitLab) over SSH and HTTPS
without any external git CLI or library dependency.
"""
